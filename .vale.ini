# Vale 配置：散文风格闸门
# 只做「套话 / AI 腔」这一类与语言无关的字面检查（自定义 AIWiki 样式），
# 不启用英文语法/可读性规则，避免对中文条目误报。中英混排空格交给 autocorrect，
# 拼写交给 cspell，中文行文交给 zhlint。
StylesPath = styles
MinAlertLevel = warning

[*.{md,mdx}]
BasedOnStyles = AIWiki
